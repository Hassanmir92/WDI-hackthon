<%= form_for(@flavour) do |f| %>
  <% if @flavour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@flavour.errors.count, "error") %> prohibited this flavour from being saved:</h2>

      <ul>
      <% @flavour.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :mood, 'Which picture best describes your current mood?' %><br>
    <%= radio_button("flavour", "mood", "Happy") %> Happy
    <%= radio_button("flavour", "mood", "Sad") %> Sad
    <%= radio_button("flavour", "mood", "Angry") %> Angry
    <%= radio_button("flavour", "mood", "Neutral") %> Neutral
  </div>
  <div class="field">
    <%= f.label :personality %><br>
    <%= radio_button("flavour", "personality", "Party") %> Party
    <%= radio_button("flavour", "personality", "Lucky") %> Lucky
    <%= radio_button("flavour", "personality", "Unlucky") %> Unlucky
    <%= radio_button("flavour", "personality", "TV") %> TV
  </div>
  <div class="field">
    <%= f.label :cheekyness %><br>
    <%= f.text_field :cheekyness %>
  </div>
  <div class="field">
    <%= f.label :transport %><br>
    <%= f.text_field :transport %>
  </div>
  <div class="field">
    <%= f.label :scenariotube %><br>
    <%= f.text_area :scenariotube %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
